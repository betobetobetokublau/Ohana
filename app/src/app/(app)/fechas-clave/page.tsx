import { ModuleStub } from '@/components/shared/module-stub';

export const metadata = { title: 'Ohana · Fechas clave' };

export default function FechasClavePage() {
  return (
    <ModuleStub
      title="Fechas clave"
      titleAccent="aniversarios + hitos"
      subtitle="Fechas únicas o recurrentes anuales (aniversarios). Notificación 7 días antes para ambos. Icono opcional, descripción."
      prdRef="§6.7 (v1.0)"
    />
  );
}
