import { ModuleStub } from '@/components/shared/module-stub';

export const metadata = { title: 'Ohana · Resumen mensual' };

export default function ResumenPage() {
  return (
    <ModuleStub
      title="Resumen mensual"
      titleAccent="ensayo del mes"
      subtitle="Cuatro narrativas generadas por Claude: Conexión, Conflicto, Logística, Intimidad. Stats strip con deltas vs mes anterior. Drop cap + tipografía magazine en iPad/web."
      prdRef="§6.14 (v1.0)"
    />
  );
}
