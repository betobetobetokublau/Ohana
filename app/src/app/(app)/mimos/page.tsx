import { ModuleStub } from '@/components/shared/module-stub';

export const metadata = { title: 'Ohana · Mimos' };

export default function MimosPage() {
  return (
    <ModuleStub
      title="Mimos"
      titleAccent="captura privada + sesión mensual"
      subtitle="Captura privada de mimos con emoción tag. Sesión presencial primer domingo del mes con reveal alternado y card-flip animation (600ms, única motion expresiva del producto). Archivo post-sesión sortable."
      prdRef="§6.9 (v1.0)"
    />
  );
}
