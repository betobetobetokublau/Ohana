import { ModuleStub } from '@/components/shared/module-stub';

export const metadata = { title: 'Ohana · Proyectos' };

export default function ProyectosPage() {
  return (
    <ModuleStub
      title="Proyectos anuales"
      titleAccent="multi-mes"
      subtitle="Proyectos con meta de ahorro opcional, accionables vinculados, timeline de seguimientos. Estados: activo / pausado / completado / cancelado."
      prdRef="§6.4 (v1.0)"
    />
  );
}
