import { ModuleStub } from '@/components/shared/module-stub';

export const metadata = { title: 'Ohana · Viajes' };

export default function ViajesPage() {
  return (
    <ModuleStub
      title="Viajes y eventos"
      titleAccent="wishlist + próximos"
      subtitle="Wishlist con interés 1-5 por partner. Próximos viajes con presupuesto, fechas, características. Revisiones post-viaje."
      prdRef="§6.6 (v1.0)"
    />
  );
}
