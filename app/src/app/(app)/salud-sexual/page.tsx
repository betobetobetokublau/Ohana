import { ModuleStub } from '@/components/shared/module-stub';

export const metadata = { title: 'Ohana · Salud sexual' };

export default function SaludSexualPage() {
  return (
    <ModuleStub
      title="Salud sexual"
      titleAccent="eventos + revisiones"
      subtitle="Eventos (en casa) y salidas (fuera). Revisiones médicas con estudios agrupados opcional. Comentarios de ambos. Privado · encriptado at rest."
      prdRef="§6.2 (v1.0)"
    />
  );
}
